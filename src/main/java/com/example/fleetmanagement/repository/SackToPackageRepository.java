package com.example.fleetmanagement.repository;

import com.example.fleetmanagement.model.SackToPackage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SackToPackageRepository extends JpaRepository<SackToPackage, Long> {
}
